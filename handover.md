# TRUST — وثيقة تسليم الجلسة (Handover)

**التاريخ:** 2 يوليو 2026
**الحالة العامة:** هيكلة MVP كاملة (Backend + Frontend) — الفرونت اند مُختبَر وشغّال فعليًا، الباك اند مكتمل الكود لكن غير مُجرَّب بالتشغيل الفعلي بسبب قيود بيئة العمل الحالية (موضّح أدناه).

هذا المستند مخصص لأي جلسة/شخص/وكيل برمجي يكمل على هذا المشروع — يلخّص القرارات المتخذة، حالة كل جزء، وما تبقى بالضبط.

---

## 1. سياق المشروع (لماذا وماذا)

TRUST منصة ذكاء أعمال تجاري: تدخل المؤسسة بياناتها (مبيعات، ربح، سيولة، أصناف) يدويًا، والنظام يحسب "مؤشر صحة الأعمال" ويولّد توصيات تنفيذية بقواعد ثابتة (بدون ذكاء اصطناعي في هذه المرحلة). المرحلة الأبعد (غير مشمولة الآن) تضيف طبقة أدمن تجمّع بيانات كل الزبائن لخلق فرص شراء جماعي وتصريف مخزون راكد بين المؤسسات.

**وثيقة الخطة الكاملة:** `trust_mvp_plan.md` (بالمجلد الجذر) — فيها كل القرارات التفصيلية (معادلات السكور، قواعد التوصيات، تصميم كل شاشة، مخطط قاعدة البيانات، الـ API). **هذا الملف هو المرجع الأساسي — أي عمل مستقبلي لازم يرجع له أولًا.**

---

## 2. ما تم إنجازه فعليًا هذه الجلسة

### 2.1 Backend — `trust-backend/` (Spring Boot 3.3.2, Java 21, H2 in-memory)

الكود **مكتمل هيكليًا وجاهز للتشغيل** لكن **لم يُبنَ فعليًا** لأن بيئة العمل هذه لا تملك وصول شبكي لـ Maven Central (فقط نطاقات محددة مسموحة). **أول شي لازم يصير بجلسة عندها إنترنت عادي: تشغيل `mvn spring-boot:run` والتأكد إنه يبني وينجح، وتصليح أي خطأ تجميع قد يظهر (لم يُختبر بمترجم Java فعلي).**

| الطبقة | الملفات | الحالة |
|---|---|---|
| Domain | `Organization, Branch, User, Category (enum), CategoryBenchmark, Item, DailyEntry, HealthScoreHistory, Recommendation` | مكتمل |
| Repository | واحد لكل entity (Spring Data JPA) | مكتمل |
| Service | `HealthScoreService` (معادلات الـ 6 أبعاد)، `RecommendationEngineService` (القواعد الـ6)، `ItemService`، `DailyEntryService`، `DashboardService` (التجميع) | مكتمل منطقيًا |
| Web | `DashboardController, ItemController, DailyEntryController, RecommendationController` + DTOs | مكتمل |
| Seed | `DataSeeder` — يبذر مؤسسة "سوبرماركت النجمة" بفرع وحيد + 7 أيام مبيعات + 6 أصناف | مكتمل |
| Security | `SecurityConfig` — كل `/api/**` مفتوح بدون مصادقة (مؤقت لمرحلة التطوير) | مبسّط عمدًا |

**نقاط مهمة لمن يكمل:**
- **لا يوجد JWT/تسجيل دخول فعلي بعد.** `User` entity موجود لكن ما في `AuthController` ولا `UserRepository` مُستخدم فعليًا. القسم 9.1 من الخطة يذكر Spring Security + JWT كهدف نهائي.
- `HealthScoreService` و `RecommendationEngineService` يعتمدان على `CategoryBenchmark` — لازم تتأكد إنه محمّل من DB (الـ Seeder يعمله) قبل ما تختبر.
- `RecommendationEngineService.estimateDailySales()` تقدير مبسّط جدًا (نسبة من الكمية الحالية حسب حالة الحركة) لأنه ما في سجل مبيعات تفصيلي لكل صنف بعد — هاي نقطة ضعف معروفة بالـ MVP، مذكورة بالكود كتعليق.
- التوصيات حاليًا **ما بتتولد تلقائيًا** — لازم تستدعي `POST /api/recommendations/regenerate?branchId=X` يدويًا (لا يوجد Scheduler مفعّل بعد رغم إنه مذكور بالخطة القسم 9.1).
- `application.yml` فيه `spring.security.user` (admin/admin) لكن هاد لواجهة أخرى غير مستخدمة فعليًا حاليًا — يمكن حذفه لاحقًا.

### 2.2 Frontend — `trust-frontend/` (React 18 + TypeScript + Vite)

**تم تشغيله فعليًا في هذه الجلسة:** `npm install` و `npm run build` نجحا بدون أخطاء (تحقق حقيقي، مو افتراض).

| الملف | الوظيفة |
|---|---|
| `src/pages/Dashboard.tsx` | الصفحة الرئيسية — تجميع كل المكونات |
| `src/components/Sidebar.tsx` | القائمة الجانبية (11 عنصر) — **كل الروابط حاليًا static، ما في routing فعلي بعد** |
| `src/components/MetricCard.tsx` | بطاقات المؤشرات العلوية (مبيعات/ربح/هامش/سيولة) |
| `src/components/HealthRadar.tsx` | راداتشارت صحة الأعمال (Recharts) |
| `src/components/SalesChart.tsx` | رسم أعمدة المبيعات (Recharts) |
| `src/components/RecommendationsList.tsx` | قائمة التوصيات |
| `src/components/DonutBreakdown.tsx` | دونات قابل لإعادة الاستخدام (مستخدم للمخزون والسيولة) |
| `src/components/AttentionTable.tsx` | جدول الأصناف المحتاجة انتباه |
| `src/api/client.ts` | axios instance + types + `fetchDashboard()` |
| `src/api/mock.ts` | **بيانات تجريبية fallback** — تُستخدم تلقائيًا إذا فشل الاتصال بالـ backend (`Dashboard.tsx` فيه `.catch()`) |
| `src/styles/theme.css` | كل الألوان/الـ RTL/الـ responsive — مطابقة لهوية التصميم الأصلي |

**نقاط مهمة لمن يكمل:**
- **يوجد صفحة واحدة فقط (الرئيسية).** باقي الـ 10 صفحات بالقائمة الجانبية (المبيعات، المخزون، المشتريات...) **غير مبنية بعد** — الـ Sidebar يعرضها لكن الضغط عليها لا يفعل شيء (لا يوجد `react-router` مُفعّل رغم أنه مثبّت في `package.json`).
- الاتصال بالـ backend عبر `VITE_API_BASE_URL` (افتراضيًا `http://localhost:8080/api`) — لازم ملف `.env` لو الـ URL مختلف.
- `organizationId=1` **مكتوب Hardcoded** بـ `Dashboard.tsx` (`fetchDashboard({ organizationId: 1 })`) — لازم يُستبدل بقيمة من جلسة تسجيل الدخول عند بناء الـ auth.
- التنبيه الأصفر ("بيانات تجريبية...") يظهر تلقائيًا لو الـ backend مو شغال — هاد سلوك مقصود للمعاينة، لازم يُحذف أو يُخفى بالإنتاج.

---

## 3. ما لم يُنجز بعد (Backlog مرتب بالأولوية)

1. **تشغيل الـ backend فعليًا** والتأكد من نجاح البناء (أول خطوة حرجة — لم تُختبر).
2. **ربط الفرونت اند بالباك اند الحقيقي** بدل الـ mock، والتحقق البصري إنه نفس شكل الأرقام.
3. **صفحة المخزون الكاملة**: فورم إضافة صنف (يستهلك `POST /api/items` الموجود فعليًا) + جدول كامل مع تبويبات سريع/متوسط/بطيء/راكد.
4. **صفحة الإدخال اليومي**: فورم مبيعات/ربح/سيولة (يستهلك `POST /api/entries/daily` الموجود فعليًا).
5. **React Router فعلي** لتفعيل التنقل بين صفحات القائمة الجانبية (المكتبة مثبّتة بالفعل بـ `package.json`).
6. **Scheduler** لتوليد التوصيات يوميًا تلقائيًا (حاليًا يدوي فقط عبر `/regenerate`).
7. **Auth حقيقي** (JWT) + شاشة تسجيل دخول + ربط `organizationId` بالمستخدم المسجّل بدل hardcoded.
8. باقي الشاشات المذكورة بالخطة (المشتريات، الربحية، السيولة، التسعير، التقارير، التنبيهات، الموردون، الإعدادات) — القسم 7 من `trust_mvp_plan.md` فيه تصميم مقترح لكل واحدة.
9. لوحة الأدمن (القسم 8 من الخطة) — لم يُبدأ بها إطلاقًا بعد (لا كود Backend ولا Frontend).

---

## 4. قرارات تصميم/معمارية مهمة (حتى ما تُعاد المناقشة)

- **لا ذكاء اصطناعي بهذه المرحلة** — التوصيات كلها if/else محسوبة (`RecommendationEngineService`)، والمستخدم صراحة طلب هيك.
- **الشات بوت أُزيل من النطاق الحالي** (كان موجود بالتصميم الأصلي، بس المستخدم طلب تجاهله مؤقتًا).
- **مقاييس صحة الأعمال الست وصيغها** موثّقة بالكامل بالقسم 4 من `trust_mvp_plan.md` وبكود `HealthScoreService` — أي تعديل على الصيغ لازم ينعكس بالمكانين معًا.
- **H2 in-memory** فقط لمرحلة التطوير — الانتقال لـ PostgreSQL (كما بالخطة الأصلية) لم يحصل بعد؛ `application.yml` لازم يتغيّر عند الانتقال.
- **التصميم البصري (الألوان/الخطوط)** يحافظ على نفس هوية لقطة الشاشة الأصلية التي زوّدنا بها المستخدم — أي إضافة صفحات جديدة لازم تستخدم نفس المتغيرات بـ `theme.css` وليس ألوان جديدة.

---

## 5. كيف تبدأ الجلسة القادمة

```bash
# فك الضغط ثم:
cd trust-backend && mvn spring-boot:run     # تحقق أولاً أنه يبني وينجح
cd trust-frontend && npm install && npm run dev
```

راجع بالترتيب: `trust_mvp_plan.md` (الخطة الكاملة) ← هذا الملف (`handover.md`) ← الكود.

الملف الأهم بالباك اند للفهم السريع: `service/HealthScoreService.java` و `service/RecommendationEngineService.java`.
الملف الأهم بالفرونت اند للفهم السريع: `pages/Dashboard.tsx`.
