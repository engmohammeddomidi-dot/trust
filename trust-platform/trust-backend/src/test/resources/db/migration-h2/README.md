نسخة من هجرات الإنتاج لأغراض الاختبار على H2 فقط.

الفروق عن الأصل، وسببها:
- V1: قيد `role` يشمل SUPPLIER منذ البداية.
- V7: مختصرة إلى إضافة عمود البريد فقط.

السبب واحد: هجرة V7 الحقيقية تُسقط قيدًا باسم يولّده Postgres تحديدًا
(`app_users_role_check`)، ولا يحمل القيد ذلك الاسم على H2 فتفشل. مجموع V1+V7 هنا
يعطي نفس المخطط النهائي الذي تعطيه النسختان الأصليتان على Postgres.

V7 الأصلية مطبَّقة سلفًا في قاعدة الإنتاج، وتعديلها يكسر بصمة Flyway - لذلك
تُنسَخ هنا ولا تُمَس هناك.

أي هجرة جديدة يجب نسخها إلى هذا المجلد أيضًا، وإلا لم يعد الاختبار يغطيها.
